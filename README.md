# sovereign-scripts

Reusable automation scripts for sovereign infrastructure.

## Scripts
- `github_batch.py` — Async repo operations with rate limiting
- `env_audit.py` — Environment variable and port scanner
- `cdn_resolver.py` — Multi-mirror package installer

## Usage
```bash
python github_batch.py --list-repos
```
